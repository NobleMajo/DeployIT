{
  "name": "go-bun-interface",
  "module": "interface.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "bun build --compile ./interface.ts --outfile ./interface",
    "format": "bun x prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
